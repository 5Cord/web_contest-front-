export type UserData = {
    username: string
    bim_coin: number
    team: number
    team_leader: boolean
}

export type TimeData = {
    time: string
    flag: boolean
}

export type StageLesson = {
    lesson: number
}